node-linker=hoisted
# Keep the pnpm content-addressable store inside the project directory.
# This ensures Metro can find all hard-linked files during the Docker build,
# because the store path will be under /usr/src/app/.pnpm-store (inside projectRoot).
store-dir=.pnpm-store
